# vessel-ops

Inventory + maintenance management for vessels.

## Monorepo
- `apps/api` — FastAPI + Postgres
- `apps/web` — Next.js
- `apps/mobile` — Expo

## Local dev

### Postgres
```bash
docker compose up -d
