# Database (required)
# Railway: Provided automatically when you add Postgres service (format: postgresql://...)
#          The code automatically converts postgresql:// to postgresql+psycopg:// for psycopg3
# Local: postgresql+psycopg://dock_ops:dock_ops@localhost:5433/dock_ops
DATABASE_URL=postgresql+psycopg://dock_ops:dock_ops@localhost:5433/dock_ops

# CORS Origins (required)
# Comma-separated list of allowed origins
# Production: https://dock-ops.com,https://www.dock-ops.com
# Local: http://localhost:3000
CORS_ORIGINS=http://localhost:3000,http://web:3000

# Auth (Clerk) - required
CLERK_SECRET_KEY=sk_test_...
CLERK_JWKS_URL=https://your-clerk-instance.clerk.accounts.dev/.well-known/jwks.json

# Email (Resend) - optional
RESEND_API_KEY=re_...
FROM_EMAIL=noreply@dock-ops.com

# Frontend URLs
# FRONTEND_URL: Used for invite email links (optional)
FRONTEND_URL=http://localhost:3000
# WEB_BASE_URL: Base URL for web app (used in billing redirects)
WEB_BASE_URL=http://localhost:3000

# Stripe (required for billing)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Stripe Price IDs (required for billing)
# Get these from Stripe Dashboard > Products
# Use test price IDs for development, live price IDs for production
STRIPE_PRICE_STARTER=price_...
STRIPE_PRICE_STANDARD=price_...
STRIPE_PRICE_PRO=price_...
STRIPE_PRICE_UNLIMITED=price_...
